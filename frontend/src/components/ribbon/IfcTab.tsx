/**
 * IFC ribbon tab — bewust leeg.
 *
 * Alle IFC-content (gegenereerde IFC4X3 STEP + IFCX JSON) staat op de
 * `/ifc` page met eigen kopieer/download knoppen per panel. Geen ribbon
 * acties nodig — pattern uit Open Calc Studio waar de IFC tab ook
 * minimal/empty is.
 */
export default function IfcTab() {
  return null;
}
