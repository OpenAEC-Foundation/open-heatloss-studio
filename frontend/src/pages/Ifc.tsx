/**
 * IFC-page — niets dan de IfcPreview split-pane (IFC4X3 STEP + IFCX JSON).
 * Geen header, geen toelichting, geen extra UI. Pattern uit Open Calc Studio.
 */
import { IfcPreview } from "../components/ifc/IfcPreview";

export function Ifc() {
  return <IfcPreview />;
}
